const Wishlist = () => {
    return (
        <section className="py-5">
            <div className="conatiner text-center fs-3">Wishlist page works!</div>
        </section>
    );
};

export default Wishlist;
